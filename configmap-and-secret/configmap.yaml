apiVersion: v1
kind: ConfigMap
metadata:
  name: app-config
data:
  APP_ENV: "production"
  LOG_LEVEL: "info"
  MAX_CONNECTIONS: "100"
  app.conf: |
    server:
      port: 8080
      timeout: 30s
  nginx.conf: |
    worker_processes auto;
    events { worker_connections 1024; }